# installed tweepy & jsonpickle
# json is the way information is communicated on the Internet
# we are going to scrape from twitter so need to download the twitter API
# With the twillter api you need to get a twitter api key, which we need to be kept secret. We use .gitignore to keep the api keys secret.

import jsonpickle
import tweepy
import pandas as pd
